#!/usr/bin/env bash

if [ -n "$CI" ]; then
    PATH_add "./scripts"
    exit 0
fi

PATH_add "/opt/homebrew/opt/ruby/bin"

layout go
layout ruby

PATH_add "./scripts"

# local development
export HUGO_CACHEDIR="${XDG_CACHE_HOME:-$TMPDIR}/hugo_cache"
