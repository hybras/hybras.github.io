#!/usr/bin/env bash

layout go
layout ruby

if [ -z "$GITHUB_ACTIONS" ]; then
    # local development
    use flake
    export HUGO_CACHEDIR="${XDG_CACHE_HOME:-$TMPDIR}/hugo_cache"
    PATH_add ".direnv/bin"
fi
